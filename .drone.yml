kind: pipeline
name: default
type: docker

trigger:
  branch:
    - main

volumes:
  - name: docker
    host:
      path: /var/run/docker.sock

steps:
  - name: push github mirror
    image: appleboy/drone-git-push
    settings:
      branch: main
      remote: https://dancheg97@github.com/fleu-io/fleu-os
      force: false
      commit: true
      ssh_key:
        from_secret: SSHKEY
